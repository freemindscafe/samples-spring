/**
 * \brief With pre-post-annotations="enabled" attribute, authorization check is enabled.
 * 
 * <ul>
 * <li>Test cases for user1 -> role1 -> permission1</li>
 * <li>With pre-post-annotations="enabled" attribute, authorization check is enabled.</li>
 * <li>Spring creates a jdk proxy as the implementation implements one interface.</li>
 * <li>If a method calls other method internally, then that call will not be through proxy.[SampleServiceImpl.java -> method6]</li>
 * </ul>
 * 
 */
package com.freemindcafe.spring.security.sample2;
